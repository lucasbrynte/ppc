default_schemeset_opts:
  test:
    poseopt:
      # GT initialization
      # init_pose_suffix: ""
      # LINEMOD
      # init_pose_suffix: _dpod_synth
      # init_pose_suffix: _dpod_real
      init_pose_suffix: _posecnn
data_sampling_scheme_defs:
  test:
    posesearch:
      ref_schemeset:
        # # Synth eval, on all frames
        # - {scheme_name: real_unoccl_all}

        # LINEMOD, DeepIM test split
        - {scheme_name: real_unoccl_deepim_test}

        # - {scheme_name: real_unoccl_deepim_test, override: {real_opts: {static_frame_idx: 194}}}
        # - {scheme_name: real_unoccl_deepim_test, override: {real_opts: {static_frame_idx: 331}}}
        # - {scheme_name: real_unoccl_deepim_test, override: {real_opts: {static_frame_idx: 720}}}

        # - {scheme_name: synth_voc}
