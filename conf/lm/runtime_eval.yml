default_schemeset_opts:
  test:
    poseopt:
      # GT initialization
      # init_pose_suffix: ""
      # LINEMOD
      init_pose_suffix: _posecnn
data_sampling_scheme_defs:
  test:
    posesearch:
      ref_schemeset:
        # LINEMOD
        - {scheme_name: real_unoccl_test_keepbg}

        # - {scheme_name: real_unoccl_test_keepbg_frame, override: {real_opts: {static_frame_idx: 194}}}
        # - {scheme_name: real_unoccl_test_keepbg_frame, override: {real_opts: {static_frame_idx: 331}}}
        # - {scheme_name: real_unoccl_test_keepbg_frame, override: {real_opts: {static_frame_idx: 720}}}

        # - {scheme_name: synth_voc}
